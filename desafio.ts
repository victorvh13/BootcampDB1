// criar uma função que soma
// a função de soma retona um boleano
// se o valor da soma for mair que 30 retona true

function soma(a: number, b: number): boolean {
    const resultado = a + b;
    return resultado >= 30;
}

console.log(soma(10, 20)); 
console.log(soma(5, 10));

  